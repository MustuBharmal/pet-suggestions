// to create the object of DogBreed
class DogBreed {
  String breed;
  List<String> characteristics;

  DogBreed(this.breed, this.characteristics);
}

// to store the Result of user in object format and then use them
class Result {
  final String dogBreed;
  final double percent;

  Result(this.dogBreed, this.percent);
}
